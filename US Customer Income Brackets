SELECT  
    CUST_ID, 
    CUST_INCOME_LEVEL 
FROM  
    SH."CUSTOMERS" 
WHERE  
    COUNTRY_ID = (SELECT COUNTRY_ID FROM SH.COUNTRIES WHERE COUNTRY_ISO_CODE = 'US') 
ORDER BY  
    CUST_INCOME_LEVEL  
