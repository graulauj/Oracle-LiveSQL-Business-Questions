SELECT  
      DISTINCT PD.PROD_NAME,  
      FLOOR(SUM(PR.AMOUNT_SOLD)) TOTAL_SALES 
FROM SH."PROFITS" PR JOIN SH."PRODUCTS" PD ON PR.PROD_ID = PD.PROD_ID 
GROUP BY PD.PROD_NAME 
ORDER BY TOTAL_SALES DESC
